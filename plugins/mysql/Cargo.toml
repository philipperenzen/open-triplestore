[package]
name = "ots-plugin-mysql"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
license = "Apache-2.0"
description = "Open Triplestore plugin: the MySQL / MariaDB datasource connector — read-only, statement-timed, streaming."

[dependencies]
ots-plugin-api = { path = "../api" }
mysql = "28"
tracing = "0.1"
