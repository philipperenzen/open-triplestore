# ─── Build output ─────────────────────────────────────────────────────────────
/target
frontend/dist/
.vite/

# ─── Dependencies ───────────────────────────────────────────────────────────
node_modules/

# ─── Local data & runtime state ─────────────────────────────────────────────
/data
*.db
*.sqlite
*.sqlite3

# The LOV corpus is never committed: the Docker image bakes a filtered extract
# of the pinned dump (the graphs listed in lov-redistributable.txt), and a full
# dump is operator-supplied or downloaded at boot. Only the derived catalog
# (lov-catalog.json.gz) and that list are committed.
/assets/vocab/lov.nq.gz

# ─── Test / CI artifacts ────────────────────────────────────────────────────
**/playwright-report/
**/test-results/

# ─── Generated benchmark reports (machine-specific; see docs/performance.md for
#     the curated numbers) ─────────────────────────────────────────────────────
/benchmark-report.md
/q_*.rq

# ─── Editor / OS / tooling noise ────────────────────────────────────────────
*.swp
*.swo
.DS_Store
.claude/
__pycache__/

# ─── Secrets — never commit (.env.example is the template) ───────────────────
.env
.env.*
!.env.example
jwt_secret

# ─── Internal docs: kept on disk, never published ────────────────────────────
# Defence in depth — these are also excluded from the public copy entirely.
SECURITY_ASSESSMENT.md
docs/audit-report-2026-04-22.md
scratch/
